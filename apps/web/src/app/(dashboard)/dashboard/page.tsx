import { Card } from "@/components/ui/card";

// Placeholder — Step 7 replaces this with the real dashboard + widgets.
export default function DashboardPage() {
  return (
    <Card>
      <p className="text-caption text-grey">Dashboard content lands in Step 7.</p>
    </Card>
  );
}
