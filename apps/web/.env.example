# Server-side only — never NEXT_PUBLIC_. Browser requests go through the BFF proxy
# at /api/proxy/[...path], so the client never sees the API origin.
API_URL="http://localhost:4000"

AUTH_COOKIE_NAME="dealport_token"

NODE_ENV=development
