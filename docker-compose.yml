services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: dealport
      POSTGRES_PASSWORD: dealport
      POSTGRES_DB: dealport
    ports:
      - "5432:5432"
    volumes:
      - dealport_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dealport -d dealport"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  dealport_postgres_data:
